<b><%= link_to "Edit this gallery", edit_gallery_path(@gallery) %></b>
<b><%= link_to "Add an image", new_gallery_image_path(@gallery) %></b>
<b><%= link_to "Delete this gallery", @gallery, method: :delete, data: { confirm: "are you sure?"} %></b>

<%= render @gallery %>
